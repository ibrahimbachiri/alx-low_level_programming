#include "3-calc.h"

/**
 * op_add - Returns the sum of a and b.
 * @a: First integer.
 * @b: Second integer.
 *
 * Return: Sum of a and b.
 */
int op_add(int a, int b)
{
    return (a + b);
}

/* Implement the other functions op_sub, op_mul, op_div, op_mod here */
